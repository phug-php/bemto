//- @node-language php
-
  if (!function_exists('copyValue')) {
    function copyValue($value) {
      if (is_array($value)) {
        return array_map('copyValue', $value);
      }
      if (is_object($value)) {
        return clone $value;
      }
      return $value;
    }
  }
